<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Fondo café -->
  <rect width="32" height="32" rx="6" fill="#352112"/>
  <!-- Clave de sol estilizada + gaviota — símbolo TDG -->
  <!-- Letra T en ámbar, estilo serif elegante -->
  <text x="16" y="24"
        font-family="Georgia, serif"
        font-size="22"
        font-weight="700"
        fill="#FFB600"
        text-anchor="middle"
        dominant-baseline="auto">T</text>
  <!-- Punto decorativo ámbar -->
  <circle cx="24" cy="10" r="3" fill="#FFB600" opacity="0.7"/>
</svg>
